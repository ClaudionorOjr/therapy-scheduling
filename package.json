{
  "name": "therapy-scheduling",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.8.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "config/*"
  ],
  "dependencies": {
    "reflect-metadata": "^0.2.2"
  }
}
